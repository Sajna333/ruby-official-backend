<!DOCTYPE html>
<html>
<head>
  <title>Home</title>
  <style>
    body {
      background-color: #f4f4f4;
      font-family: sans-serif;
      padding: 20px;
    }
    header {
      background-color: olive;
      color: white;
      padding: 10px;
      text-align: center;
    }
    .products {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      margin-top: 20px;
    }
    .product {
      border: 1px solid #ccc;
      padding: 10px;
      width: 200px;
      background: white;
    }
  </style>
</head>
<body>
  <header>
    <h1>Ruby Official Store</h1>
  </header>

  <div class="products">
    <% products.forEach(product => { %>
      <div class="product">
        <h3><%= product.name %></h3>
        <p>₹<%= product.price %></p>
        <a href="/product/<%= product._id %>">View Details</a>
      </div>
    <% }) %>
  </div>
</body>
</html>
